# =============================================================================
# music-generation — shared core dependencies
# =============================================================================
numpy>=1.24,<3
music21>=9.1,<11

# =============================================================================
# hmm — form-conditioned HMM music generation
# =============================================================================
scipy>=1.10,<2
scikit-learn>=1.3,<2
python-Levenshtein>=0.21,<1
pyyaml>=6.0,<7
mido>=1.3,<2

# =============================================================================
# hmm — optional: diagnostics and chart generation
# =============================================================================
matplotlib>=3.7,<4
pandas>=2.0,<3
seaborn>=0.12,<1

# =============================================================================
# vae_transformer — DVAE + Transformer generation pipeline
# =============================================================================
torch>=2.0,<3

# =============================================================================
# vae_transformer — optional: architecture diagram rendering
# =============================================================================
# Pillow>=10.0,<12
