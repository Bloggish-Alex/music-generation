# Codec Fidelity V2 Metrics

V2 reports melody and bass exact pitch/state matches, harmony active-event multiset precision, recall, F1 and cardinality, context chroma, discrete lane chroma, register median error, and explicit empty or unavailable row counts. All metrics are independent and the report defaults to `MONITOR` when inputs are complete.
