# Teacher-Forced Trajectory Metrics

Teacher-forced evaluation uses reference history. Compare it with free-running evaluation only when checkpoint, song, primer, split, seed, initialization, and sampling noise are identical.
