# Anchor Transport Metrics

Verify exact lineage and equality of bar-level absolute anchors across source, encoding, export, trajectory inputs, and rendering inputs. An anchor mismatch is a contract error, not a prediction error.
