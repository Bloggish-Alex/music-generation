# Teacher-Forced Future-Position Metrics v1

Report each requested future-bar position independently. Position-specific error reveals whether a system fails immediately or accumulates drift over rollout depth.
