# Attribution Metrics

Attribution compares paired experimental arms to identify where an observed difference enters the information path. It reports evidence by boundary and metric family; it does not assign a single percentage of final musical quality to any component.

Paired artifacts must have identical dataset, split, song, primer, seed, initialization, sampling, and checkpoint lineage. A missing or mismatched identity field makes the comparison unavailable.
